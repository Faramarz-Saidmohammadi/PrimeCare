import { DoctorCard } from "@/components/DoctorCard";
import { PageHero } from "@/components/PageHero";
import { getDoctors } from "@/lib/content";
export const metadata={title:"Our Doctors"};
export default async function DoctorsPage(){const doctors=await getDoctors();return <><PageHero eyebrow="Clinical team" title="Care areas supported by the platform" text="These role-based profiles demonstrate how a clinic can present verified clinicians, qualifications, services, and appointment options."/><section className="section"><div className="container doctor-grid">{doctors.map(d=><DoctorCard doctor={d} key={d.slug}/>)}</div></section><section className="section section-muted"><div className="container"><div className="team-note"><h2>Verified profiles required for deployment</h2><p>Before a real clinic launches the platform, every clinician name, qualification, registration, experience statement, and photograph must be checked and replaced through the administration workspace.</p></div></div></section></>}
